#ifndef ACCUMULATE_FLOAT_PERIOD_H

#define ACCUMULATE_FLOAT_PERIOD_H

/**
 * Appends a period to the float currently being accumulated.  Halts execution
 * should this not be expected.
 */
void accumulate_float_period(void);

#endif

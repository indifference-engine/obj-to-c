#ifndef ACCUMULATE_FLOAT_HYPHEN_H

#define ACCUMULATE_FLOAT_HYPHEN_H

/**
 * Appends a minus to the float currently being accumulated.  Halts execution
 * should this not be expected.
 */
void accumulate_float_hyphen(void);

#endif

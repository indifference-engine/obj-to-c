#ifndef TEXTURE_COORDINATES_H

#define TEXTURE_COORDINATES_H

#include <stdlib.h>

/**
 * The location of the texture coordinate on the X axis.
 */
extern float *texture_coordinate_u;

/**
 * The location of the texture coordinate on the Y axis.
 */
extern float *texture_coordinate_v;

/**
 * The number of texture coordinates.
 */
extern size_t number_of_texture_coordinates;

#endif

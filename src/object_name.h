#ifndef OBJECT_NAME_H

#define OBJECT_NAME_H

#include <stdlib.h>

/**
 * The characters currently accumulated into an object name.
 */
extern char *object_name;

/**
 * The number of characters currently accumulated into an object name.
 */
extern size_t object_name_length;

#endif

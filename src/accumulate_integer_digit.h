#ifndef ACCUMULATE_INTEGER_DIGIT_H

#define ACCUMULATE_INTEGER_DIGIT_H

/**
 * Appends a digit to the integer currently being accumulated.  Halts execution
 * should this not be expected.
 * @param digit The digit to append.
 */
void accumulate_integer_digit(const int digit);

#endif

#ifndef ACCUMULATE_FLOAT_DIGIT_H

#define ACCUMULATE_FLOAT_DIGIT_H

/**
 * Appends a digit to the float currently being accumulated.  Halts execution
 * should this not be expected.
 * @param digit The digit to append.
 */
void accumulate_float_digit(const int digit);

#endif

#ifndef ACCUMULATE_FLOAT_H

#define ACCUMULATE_FLOAT_H

/**
 * Reads the accumulated float and resets state.  Halts execution should this
 * not be expected.
 * @return The accumulated float.
 */
float accumulate_float(void);

#endif

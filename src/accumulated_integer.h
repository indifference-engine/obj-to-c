#ifndef ACCUMULATED_INTEGER_H

#define ACCUMULATED_INTEGER_H

#include <stdlib.h>

/**
 * The characters currently accumulated into an integer.
 */
extern char *accumulated_integer;

/**
 * The number of characters currently accumulated into an integer.
 */
extern size_t accumulated_integer_length;

#endif

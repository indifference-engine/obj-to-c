#ifndef INDICES_H

#define INDICES_H

#include <stdlib.h>

/**
 * The vertex indices in the current object.
 */
extern size_t *index_v;

/**
 * The texture coordinate indices in the current object.
 */
extern size_t *index_vt;

/**
 * The number of indices in the current object.
 */
extern size_t number_of_indices;

#endif

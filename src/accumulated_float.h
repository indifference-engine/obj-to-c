#ifndef ACCUMULATED_FLOAT_H

#define ACCUMULATED_FLOAT_H

#include <stdlib.h>

/**
 * The characters currently accumulated into an float.
 */
extern char *accumulated_float;

/**
 * The number of characters currently accumulated into an float.
 */
extern size_t accumulated_float_length;

#endif

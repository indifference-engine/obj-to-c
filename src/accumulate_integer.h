#ifndef ACCUMULATE_INTEGER_H

#define ACCUMULATE_INTEGER_H

/**
 * Reads the accumulated integer and resets state.  Halts execution should this
 * not be expected.
 * @return The accumulated integer.
 */
int accumulate_integer();

#endif

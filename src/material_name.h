#ifndef MATERIAL_NAME_H

#define MATERIAL_NAME_H

#include <stdlib.h>

/**
 * The characters currently accumulated into an material name.
 */
extern char *material_name;

/**
 * The number of characters currently accumulated into an material name.
 */
extern size_t material_name_length;

#endif
